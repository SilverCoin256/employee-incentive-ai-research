# Table Manifest

The final package includes the following table files:

- **`model_benchmark_metrics.csv`**
- **`clustering_metrics.csv`**
- **`fairness_audit.csv`**
- **`top_permutation_importance.csv`**
- **`cluster_profiles.csv`**
- **`seed_robustness.csv`**

The actual CSV files are present in:

`submission_exports/final_submission_package/tables/`

For final upload, include the full `tables/` folder from `submission_exports/final_submission_package/`.
