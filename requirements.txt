# Reproducible environment for pipeline.py
# Python 3.11 recommended. Tested versions noted in comments.

catboost>=1.2.7        # tested 1.2.10
scikit-learn>=1.5      # tested 1.9.0
numpy>=1.26
pandas>=2.0
scipy>=1.11
matplotlib>=3.8

# shap is not required: pipeline.py calls CatBoost's native
# get_feature_importance(type="ShapValues"), which is exact TreeSHAP
# computed internally without the shap package.
