# Reproducible environment for run_real_empirical_pipeline.py
# Python 3.11 recommended. Tested versions noted in comments.

catboost>=1.2.7        # tested 1.2.10
scikit-learn>=1.5      # tested 1.9.0
numpy>=1.26
pandas>=2.0
scipy>=1.11
matplotlib>=3.8

# Optional. If shap is absent, the pipeline uses CatBoost's native
# (exact TreeSHAP) Shapley values instead.
# shap>=0.44
